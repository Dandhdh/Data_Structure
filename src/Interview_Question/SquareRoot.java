import java.util.Scanner;/** * 爱奇艺编程题 * * 平方根问题 * * 考虑定义在两正整数上的函数SSR(平方根之和的平方):SSR(A, B) = (sqrt(A) + sqrt(B))^2。牛牛对函数值为整数的情况很感兴趣。现在给定整数n和m,请帮助牛牛计算有序对(A, B)的数量, 满足1 ≤ A ≤ n, 1 ≤ B ≤ m而且SSR(A, B)是一个整数。 * 输入描述: * 输入包括两个整数n和m(1 ≤ n ≤ 10^5, 1 ≤ m ≤ 10^5) * * * 输出描述: * 输出一个整数,表示满足条件的有序对对数。 * 示例1 * 输入 * 3 8 * 输出 * 5 */public class SquareRoot{    private static int[] pend = new int[100005];    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int n = sc.nextInt();        int m = sc.nextInt();        int sqrtN = (int)Math.sqrt(n);        int sqrtM = (int)Math.sqrt(m);        long ans = sqrtN * sqrtM;        int maxValue = n>m?n:m;        getPend(maxValue);        for(int i=2;;i++)        {            if(pend[i]==1) continue;            if(n/i==0 || m/i==0) break;            int nt=n/i,mt=m/i;            int snt=(int)Math.sqrt(nt),smt=(int)Math.sqrt(mt);            ans+=snt*smt;        }        System.out.println(ans);        return ;    }    static void getPend(int m)    {        for(int i=2,rec=i*i; rec<=m; i++,rec=i*i)        {            if(pend[rec]==1) continue;            for(int j=rec;j<=m;j+=rec)                pend[j]=1;        }    }}