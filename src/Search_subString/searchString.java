package Search_subString;/** * 暴力子字符串查找 * * 在长度N的文本中查找长度为M的模式，需要 */public class searchString {    public static int search(String pattern, String txt){        int M = pattern.length();        int N = txt.length();        for (int i=0; i<=N-M; i++){            int j ;            for (j=0; j<M; j++){                if (txt.charAt(i+j) != pattern.charAt(j)){                    break;                }            }            if (j==M){                return i;   // 找到匹配，返回子字符串的索引            }        }        return -1;   // 没有找到匹配的    }    public static void main(String[] args) {        String pat = "wxc";        String txt = "dyyasndnadlknaskdnwxc";        System.out.println(search(pat,txt));    }}